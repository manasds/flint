{
  "name": "flint-signaling",
  "version": "2.0.0",
  "private": true,
  "description": "Cloudflare Worker + Durable Object signaling for Flint P2P file sharing.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test test/room-logic.test.ts",
    "validate": "pnpm typecheck && pnpm test"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260823.1",
    "@types/node": "^20.19.43",
    "tsx": "^4.23.0",
    "typescript": "^5.9.2",
    "wrangler": "^4.32.0"
  }
}
